//声明
function 声明(true) {
log（“使用：嗨，欢迎使用PC版，auto.js pc IDE软件”）；
log（“授权：本软件使用完全免费”）；
log（“版权：没有”）；
log（“转载：请随便传播”）；
log（“联系QQ：893020302”）；
} 

// 教程
function 调试教程(true) {
log（“第一步：开启服务器”）；
log（“第二步：打开手机auto.js，输入本机ip地址”）；
log（“第一步：开始调试”）；
} 